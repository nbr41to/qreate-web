import React from 'react';
import { Button } from 'react-bootstrap';

/**
 * Primary UI component for user interaction
 */
export const ButtonBase = () => {
  return <Button onClick={() => {}}>Sample</Button>;
};
